class AddProjectToCodeChunks < ActiveRecord::Migration[8.1]
  def change
    # This migration was already completed manually via SQL
    # The project_id column and foreign key already exist
    # No action needed
  end
end
